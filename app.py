import streamlit as st
import pandas as pd
import xml.etree.ElementTree as ET

# --- 1. إعدادات الصفحة ---
st.set_page_config(page_title="تحصيل شان الحديثة", layout="wide")

st.title("💸 مديونية العملاء - مطابقة ميزان المراجعة بالأسماء")
st.markdown("### المستهدف النهائي: **218,789.96** ر.س")

# --- 2. دالة القراءة المباشرة ---
def load_data(file):
    if file is None: return None
    file.seek(0)
    try:
        tree = ET.parse(file)
        root = tree.getroot()
        data = []
        for row in root:
            data.append({child.tag: child.text for child in row})
        return pd.DataFrame(data)
    except Exception as e:
        st.error(f"فشل القراءة: {e}")
        return None

# --- 3. القائمة الجانبية ---
with st.sidebar:
    st.header("📂 استيراد البيانات")
    f_ledger = st.file_uploader("ارفع ملف LedgerBook.xml", type=['xml'], key="ledger_named_v1")

# --- 4. معالجة البيانات بناءً على أسماء ميزان المراجعة ---
if f_ledger:
    df_raw = load_data(f_ledger)
    if df_raw is not None:
        try:
            # تحويل المبالغ
            df_raw['Dr'] = pd.to_numeric(df_raw['Dr'], errors='coerce').fillna(0)
            df_raw['Cr'] = pd.to_numeric(df_raw['Cr'], errors='coerce').fillna(0)
            
            # --- قائمة الأسماء المستخرجة من ميزان المراجعة المرفوع ---
            target_names = [
                "شركة فاطمة صالح بامسق التجارية", "شركة الريادة العربية التجارية", "شركة أصل الشرق لقطع غيار السيارات فرع 14",
                "شركة ركن الأمجاد المتحدة للتجارة", "شركة موجود المتحدة للتجارة", "مؤسسة وتين الغربية التجارية",
                "شركة بن شيهون البركة التجارية فرع 14", "مؤسسة الاختيار الامثل لقطع غيار السيارات", "مؤسسة علي فريد علي المرهون (عالم ام جي)",
                "شركة خالد حامد سالم المحمادي التجارية", "مؤسسة جود الجزيرة لقطع غيار السيارات", "مؤسسة نوادر المحرك لقطع الغيار",
                "مؤسسة الابداع الصيني لقطع غيار السيارات", "مؤسسة عواطف سالم باجابر", "شركة ارض الذهب للذهب والمجوهرات",
                "شركة بن شيهون البركة التجارية فرع النزهة", "مؤسسة الراقي العالمي لقطع الغيار", "مؤسسة الاقبال التجارية",
                "مؤسسة الامداد الحصري التجارية", "مؤسسة رمز الصفوة لقطع غيار السيارات", "مؤسسة رواد الجودة لقطع الغيار",
                "مؤسسة وفاق الفرسان للتجارة", "مؤسسة الوفاء الخالدة لتجارة الجملة والتجزئة", "شركة أصل المصدر الرائدة لقطع غيار السيارات",
                "مؤسسة عبود صالح باحشوان لقطع غيار السيارات", "شركة اسطورة الشرق التجارية", "مؤسسة الشامل المتميز لقطع غيار السيارات",
                "شركة أصل الشرق لقطع غيار السيارات فرع بني مالك", "شركة ركن الصناعية للتجارة", "مؤسسة رواد اسيا لقطع غيار السيارات",
                "شركة عزيز كرامة الحداد لقطع الغيار", "شركة قلب الصقر للتجارة", "شركة الأخوه المتحدون لقطع غيار السيارات",
                "شركة الاتحاد المتطورة للتجارة", "مؤسسة رمز الصفوة لقطع غيار السيارات 3 فرع محايل", "شركة تمكين الخليجية للتجارة",
                "شركة المسارات العربية التجارية", "مؤسسة حلول المركبة لقطع غيار السيارات", "مؤسسة ابوبكر عبود ابوبكر بن عفيف لقطع غيار السيارات",
                "مؤسسة رمز الصفوة لقطع غيار السيارات المركز الصيني بني مالك", "شركة السلام التجارية", "شركة المحرك الأفضل لتجارة الجملة والتجزئة",
                "نواف احمد", "مؤسسة الزعيم واحد لقطع غيار السيارات", "حسين حسن أبوبكر باقيس", "الأستاذ أحمد سعيد سالم باوزير",
                "مديونية الجدعاني (عبد الله)", "مسحوبات شخصية (العائلة)", "منقذة لقطع غيار السيارات", "MWZ AUTO PARTS",
                "CHONGQING YIJIA CO", "GUANGZHOU TIANYI AUTOMOTIVE PARTS CO", "CASSTIME(HK) TECHNOLOGY LIMITED"
            ]
            
            # التجميع والبحث بالاسم
            summary = df_raw.groupby('LedgerName').agg({'Dr': 'sum', 'Cr': 'sum'}).reset_index()
            summary['Balance'] = summary['Dr'] - summary['Cr']
            
            # فلترة الأسماء المطابقة فقط
            final_debtors = summary[summary['LedgerName'].isin(target_names)].sort_values('Balance', ascending=False)
            # استبعاد الأرصدة غير المدينة (لضمان تطابق المديونية)
            final_debtors = final_debtors[final_debtors['Balance'] > 0]

            # --- 5. عرض النتائج ---
            total_val = final_debtors['Balance'].sum()
            
            c1, c2 = st.columns(2)
            c1.metric("إجمالي المديونية (المطابقة للأسماء)", f"{total_val:,.2f} ر.س")
            c2.metric("عدد الحسابات المكتشفة", f"{len(final_debtors)}")
            
            target = 218789.96
            st.warning(f"المستهدف في تقرير البرنامج: {target:,.2f} ر.س")
            
            st.divider()
            st.subheader("📋 كشف الأرصدة (بناءً على قائمة ميزان المراجعة)")
            st.dataframe(
                final_debtors[['LedgerName', 'Balance']], 
                column_config={"Balance": st.column_config.NumberColumn("الرصيد", format="%.2f")},
                use_container_width=True, 
                height=600
            )
            
        except Exception as e:
            st.error(f"خطأ في المعالجة: {e}")
else:
    st.info("💡 ارفع ملف LedgerBook.xml للبدء.")
